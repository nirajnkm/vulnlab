bind = '0.0.0.0:5000'  # Change this to your desired IP and port
workers = 2  # Adjust this based on your expected traffic
accesslog = 'access.log'  # Enable access logging (optional)
errorlog = 'error.log'  # Enable error logging (optional)
daemon = True  # Run Gunicorn in the background (optional)
keepalive = 2  # Set timeout for idle connections (optional)
loglevel = 'info'  # Set logging level (debug, info, warning, etc.)